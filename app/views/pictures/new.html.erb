<%= form_with(model: @picture, local: true) do |form| %>
  <%= form.label :image %>
  <%= form.file_field :image %> <br>
  <%= form.label :sentence %>
  <%= form.text_area :sentence %>
  <%= form.submit %>
<% end %>
